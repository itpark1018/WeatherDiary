spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: WeatherDiary

myapp:
  openapi:
    api-key: api-key


